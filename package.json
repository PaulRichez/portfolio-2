{
  "name": "portfolio",
  "version": "2.0.0",
  "description": "Portfolio — Angular 19 + Strapi 5 (chatbot IA via Zhipu, sans RAG)",
  "scripts": {
    "start": "npm run dev & npm run client",
    "dev": "cd portfolio-server && npm run dev",
    "client": "cd portfolio-client && npm start",
    "install": "npm install && cd portfolio-server && npm install && cd ../portfolio-client && npm install",
    "docker:build": "docker compose build",
    "docker:start": "docker compose up -d",
    "docker:start-postgres": "docker compose --profile postgres up -d",
    "docker:stop": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:status": "docker compose ps",
    "docker:clean": "docker compose down -v && docker system prune -f",
    "sync:landings": "node scripts/sync-landings.mjs"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}