{
  "collectionName": "components_me_codings",
  "info": {
    "displayName": "coding",
    "icon": "code"
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "icon": {
      "type": "string",
      "required": true
    },
    "level": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "beginner",
        "Intermediate",
        "advanced",
        "expert"
      ]
    },
    "category": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "frontend_languages",
        "frontend_frameworks",
        "backend",
        "databases",
        "devops_tools",
        "tools",
        "other_languages"
      ]
    }
  },
  "config": {}
}
